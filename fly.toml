app = "neurohire-nz"
primary_region = "syd"

[build]

[env]
  NODE_ENV = "production"
  DATA_DIR = "/data"

[http_service]
  internal_port = 3853
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/api/health"

[[mounts]]
  source = "neurohire_data"
  destination = "/data"